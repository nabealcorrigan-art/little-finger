version: '3.8'

services:
  little-finger:
    build: .
    container_name: little-finger-monitor
    ports:
      - "5000:5000"
    volumes:
      - ./config.local.json:/app/config.json
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
